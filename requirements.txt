# EnderTrack — Python dependencies
#
# All dependencies are included in vendor/ (zero install required).
# This file is provided if you prefer to install via pip:
#
#   pip install -r requirements.txt

flask>=2.0
flask-cors>=3.0
pyserial>=3.5
